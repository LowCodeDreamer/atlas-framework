---
type: service
name: {{SERVICE_NAME}}
status: {{SERVICE_STATUS}}
created_at: {{INIT_DATE}}
language: {{SERVICE_LANGUAGE}}
runtime: {{SERVICE_RUNTIME}}
exposes: {{SERVICE_EXPOSES}}
---

# {{SERVICE_NAME_TITLE}} Service

{{SERVICE_PURPOSE}}

## Purpose

(One paragraph. What does this service do for the instance? Why is it long-running rather than a bounded project?)

## Surface

(How does the rest of the instance talk to this service? HTTP, CLI, Unix socket, message bus?)

## Operations

- **Start:** `{{SERVICE_START_CMD}}`
- **Stop:** `{{SERVICE_STOP_CMD}}`
- **Logs:** `{{SERVICE_LOGS_CMD}}`
- **Health:** `{{SERVICE_HEALTH_CMD}}`

## Dependencies

(Other services, databases, MCP servers, secrets this service needs.)

## Parent

This service is operated by the {{INSTANCE_NAME}} instance. See `{{INSTANCE_HOME}}/CLAUDE.md` for instance-wide context.
